import 'package:flutter/material.dart';

class AddAd2 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      appBar: AppBar(
        title: Text('hello'),
      ),
      body: Text('Adding photos'),
    );
  }
}